---
- name: COLLECT PRIMARY IP ADDRESS OF MACHINE
  debug:
    msg: "{{ ansible_all_ipv4_addresses | list | ipaddr('public') | first }}"
  register: ip_validation

- name: VALIDATE PRIMARY IP OF MACHINE IS A PUBLIC IP
  assert:
    that:
      - "{{ ip_validation | length }} > 0"
